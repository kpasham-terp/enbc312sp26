# Homework 3 - ENBC312 Spring 2026

## Problem 1: 
Used pointer ints to assign variables to memory addresses of array elements. Then, swapping them by using a temp variable and printing out each element with a for loop.

## Problem 2:
Used cin and cout to get inputs and place them in an integer array. After initializing a pointer to the array, I used a for loop to add each element to the sum and printed it out at the end.

## Problem 3:
Created a helper function that takes two arguments; a pointer to an integer array and the size of aforementioned array. Then, it assigns a start and end pointer to the respective elements in the array and swaps them with the help of a temp variable. Afterwards, the pointers increment/decrement respectively until they've gone halfway. Since the pointers in the memory are the ones getting updated, there is no need to return any variable. Simply print out the array using a for loop afterwards.

## Problem 4:
Use cin and cout to receive two input variables and then used the '&' operator on both to call a helper function that uses the same logic to swap two pointers using a temp variable. Print out the values of a and b before and after using the function.